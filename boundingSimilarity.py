# We compare the length(the longer side) and the width(the shorter side)

def get_2d_width_length_similarity(path_3d, path_2d):
    #Open the 2d image
    #Mask it
    #Get four values (top, bottom, left most, right most indices)
    #Return the length and width